"""Compare our stats with website values"""

import unittest

from stats.games import agg_team_stats, annotate_game_results, annotate_computed_stats


class TestCompareWithWebsite(unittest.TestCase):
    def test_foo(self):
        self.assertFalse(True)
